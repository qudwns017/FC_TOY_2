spring:
  datasource:
    url: jdbc:mysql://localhost:3306/traveler?useSSL=false&serverTimezone=UTC
    username: root
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  config-location: classpath:mybatis-config.xml