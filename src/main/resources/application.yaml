spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel?useSSL=false&serverTimezone=UTC
    username: root
    password: kms900326
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration-properties:
    defaultStatementTimeout: 25